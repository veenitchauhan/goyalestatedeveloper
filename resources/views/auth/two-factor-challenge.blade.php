@extends('layouts.admin')
@section('title','Two-factor verification')
@section('content')<section class="auth-card"><h1>One more check.</h1><p>Enter the six-digit code from your authenticator app.</p><form method="post" action="{{ route('two-factor.login.store') }}">@csrf<label for="code">Authentication code</label><input id="code" name="code" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" autocomplete="one-time-code" required autofocus><button>Verify and sign in</button></form><details><summary>Use a recovery code</summary><form method="post" action="{{ route('two-factor.login.store') }}">@csrf<label for="recovery_code">Recovery code</label><input id="recovery_code" name="recovery_code" required autocomplete="off"><button>Use recovery code</button></form></details></section>@endsection
