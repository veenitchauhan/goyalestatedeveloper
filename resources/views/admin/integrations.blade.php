@extends('layouts.admin')
@section('title','Integrations')
@section('content')
<p class="eyebrow">CONNECTION STATUS</p><h1>Integrations</h1><section><h2>Call & WhatsApp lead adapter</h2><p>{{ $enabled ? 'Signed lead intake enabled.' : 'Disabled. No external provider is connected through this adapter.' }}</p><p>Receives normalized, consented lead submissions from a configured provider bridge. Provider-specific webhook translation, credentials and sandbox verification are required before live use.</p><p>Inbound endpoint: <code>/integrations/lead-events</code></p><p>The bridge signs the exact JSON body with HMAC-SHA256 using <code>timestamp.body</code>, sending Unix seconds in <code>X-Bridge-Timestamp</code> and the lowercase hexadecimal signature in <code>X-Bridge-Signature</code>. Requests expire after five minutes. Each lead submission needs a unique <code>event_id</code>; retries must preserve the exact body.</p><p>Required JSON fields: event_id, channel (call or whatsapp), name, email, location, message, consent (true). Optional: phone, duration_seconds, outcome, campaign. Repeated IDs with different bodies are rejected. Accepted submissions enter the CRM unassigned.</p><p>Activation uses the server settings LEAD_BRIDGE_ENABLED and a secret of at least 32 characters in LEAD_BRIDGE_SECRET. Secrets are never displayed here.</p></section><section><h2>Approved knowledge boundary</h2><p>Any future assistant must use published evidence, treat source text as data, and refer unanswered questions to a human. No automated answers or outbound WhatsApp messages are enabled.</p><a href="{{ route('admin.integrations.knowledge') }}">Inspect published source index →</a></section><section><h2>Recent accepted events</h2>@forelse($recent as $event)<p>{{ $event->channel }} · {{ $event->external_id }} · {{ $event->created_at }} UTC</p>@empty<p>No provider events received.</p>@endforelse</section><section><h2>Third-party tracking</h2><p>Google, Meta and call-provider tracking are not connected. This checkpoint uses first-party, opt-in analytics.</p></section>
@endsection
