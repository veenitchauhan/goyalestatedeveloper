@extends('layouts.admin')
@section('title','Future developments')
@section('content')
<p class="eyebrow">FUTURE DEVELOPMENT</p><h1>Developments & inventory</h1><section><h2>Public visibility</h2><p>{{ \App\Services\DevelopmentContent::enabled() ? 'Enabled: approved developments can appear publicly.' : 'Disabled: public development routes, navigation and discovery are hidden.' }}</p><form method="post" action="{{ route('admin.developments.toggle') }}">@csrf<input type="hidden" name="version" value="{{ \App\Models\SiteSetting::where('key','future_developments')->first()?->data['version']??0 }}"><select name="enabled" aria-label="Public development visibility"><option value="0" @selected(!\App\Services\DevelopmentContent::enabled())>Keep public module disabled</option><option value="1" @selected(\App\Services\DevelopmentContent::enabled())>Enable approved public developments</option></select><button>Save visibility</button></form></section><a class="button" href="{{ route('admin.developments.create') }}">Add development</a><div class="cards">@forelse($entries as $entry)<section><h2>{{ $entry->title }}</h2><p>{{ str($entry->status)->headline() }}</p><a href="{{ route('admin.developments.edit',$entry) }}">Manage development →</a></section>@empty<section><h2>No developments added</h2><p>Prepare drafts here when approved details are available.</p></section>@endforelse</div>{{ $entries->links('pagination') }}
@endsection
