@extends('layouts.admin')
@section('title','Job editor')
@section('content')
<p class="eyebrow">CAREERS & OPENINGS</p><h1>{{ $entry->exists ? $entry->title : 'Add an opening' }}</h1><a href="{{ route('admin.jobs.index') }}">← All openings</a>
<form method="post" action="{{ $entry->exists ? route('admin.jobs.update',$entry) : route('admin.jobs.store') }}">@csrf @if($entry->exists) @method('PUT') @endif<input type="hidden" name="version" value="{{ $revision?->version??0 }}"><section><h2>Opening details</h2><div class="form-grid"><div><label for="title">Job title</label><input id="title" name="title" value="{{ old('title',$payload['title']??'') }}" ></div><div><label for="slug">URL slug</label><input id="slug" name="slug" value="{{ old('slug',$payload['slug']??'') }}" @readonly($entry->exists)></div><div><label for="department">Department</label><input id="department" name="department" value="{{ old('department',$payload['department']??'') }}" ></div><div><label for="location">Location</label><input id="location" name="location" value="{{ old('location',$payload['location']??'') }}" ></div><div><label for="experience">Experience</label><input id="experience" name="experience" value="{{ old('experience',$payload['experience']??'') }}" ></div><div><label for="salary">Salary (optional)</label><input id="salary" name="salary" value="{{ old('salary',$payload['salary']??'') }}" ></div></div><label for="job_type">Job type</label><select name="job_type" id="job_type">@foreach(\App\Services\CareerContent::TYPES as $option)<option @selected(old('job_type',$payload['job_type']??'')===$option)>{{ $option }}</option>@endforeach</select><label for="employment_type">Employment type</label><select name="employment_type" id="employment_type">@foreach(\App\Services\CareerContent::EMPLOYMENT as $option)<option @selected(old('employment_type',$payload['employment_type']??'')===$option)>{{ $option }}</option>@endforeach</select><label for="deadline">Application deadline</label><input type="date" id="deadline" name="deadline" value="{{ old('deadline',$payload['deadline']??'') }}"><label for="salary_public">Show salary publicly</label><select id="salary_public" name="salary_public"><option value="0" @selected(!old('salary_public',$payload['salary_public']??false))>No</option><option value="1" @selected(old('salary_public',$payload['salary_public']??false))>Yes</option></select></section><section><h2>Job location for search</h2><p>Enter verified location details. Country code, city and an application deadline are needed for job structured data.</p><div class="form-grid">@foreach(['address_country'=>'Country code (two uppercase letters)','address_locality'=>'City / locality','address_region'=>'State / region','postal_code'=>'Postal code','street_address'=>'Street address'] as $key=>$label)<div><label for="{{ $key }}">{{ $label }}</label><input id="{{ $key }}" name="{{ $key }}" value="{{ old($key,$payload[$key]??'') }}"></div>@endforeach</div></section><section><h2>Role description</h2><label for="description">Description</label><textarea id="description" name="description" rows="4">{{ old('description',$payload['description']??'') }}</textarea><label for="responsibilities">Responsibilities</label><textarea id="responsibilities" name="responsibilities" rows="4">{{ old('responsibilities',$payload['responsibilities']??'') }}</textarea><label for="requirements">Requirements</label><textarea id="requirements" name="requirements" rows="4">{{ old('requirements',$payload['requirements']??'') }}</textarea><label for="skills">Skills</label><textarea id="skills" name="skills" rows="4">{{ old('skills',$payload['skills']??'') }}</textarea><label for="education">Education</label><textarea id="education" name="education" rows="4">{{ old('education',$payload['education']??'') }}</textarea><label for="source_note">Internal approval reference</label><textarea id="source_note" name="source_note" rows="4">{{ old('source_note',$payload['source_note']??'') }}</textarea><input type="hidden" name="verified" value="0"><label><input type="checkbox" name="verified" value="1" @checked(old('verified',$payload['verified']??false))> The opening and its details are approved.</label><button>Save job draft</button></section></form>
@if($entry->exists)<section><h2>Review & publication</h2><p>{{ str($entry->status)->headline() }} · Version {{ $revision->version }}</p><a href="{{ route('admin.jobs.preview',$entry) }}">Preview saved draft</a><form method="post" action="{{ route('admin.jobs.transition',$entry) }}">@csrf<input type="hidden" name="version" value="{{ $revision->version }}">
@if(in_array($entry->status,['draft','unpublished']))<button name="action" value="review">Submit for review / reopen</button>@endif
@can('jobs.publish')
@if($entry->status==='review')<button name="action" value="approve">Approve</button>@endif
@if($entry->status==='approved')<button name="action" value="publish">Publish opening</button>@endif
@if(in_array($entry->status,['review','approved','scheduled']))<button name="action" value="return" class="quiet">Return to draft</button>@endif
@if($entry->published_revision_id)<button name="action" value="unpublish" class="quiet">Close opening / unpublish</button>@endif
@if($entry->status==='archived')<button name="action" value="restore">Restore as draft</button>@else<button name="action" value="archive" class="quiet">Archive</button>@endif
@endcan
</form></section>@endif
@endsection
