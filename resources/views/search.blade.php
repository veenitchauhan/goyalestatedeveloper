@extends('layouts.public')
@section('content')
<section class="corporate-hero"><div class="corporate-container"><p class="eyebrow">EXPLORE</p><h1>Search the website</h1><p class="lead">Find projects, services, locations, insights, answers and current openings.</p></div></section><div class="corporate-container corporate-detail"><form method="get" class="project-filters"><div><label for="q">Search</label><input name="q" id="q" value="{{ $q }}" minlength="2" maxlength="150" required></div><button class="button ink">Search</button></form><div class="project-cards">@forelse($results as $item)<article><p class="eyebrow">{{ \App\Services\KnowledgeContent::TYPES[$item['type']]??str($item['type'])->headline() }}</p><h2><a href="{{ $item['url'] }}">{{ $item['title'] }}</a></h2></article>@empty<p>{{ mb_strlen(trim($q))<2 ? 'Enter at least two characters to search.' : 'No matching published content found.' }}</p>@endforelse</div>@if($results->count()===100)<p>Showing the first 100 matches. Refine your search for more specific results.</p>@endif</div>
@endsection
